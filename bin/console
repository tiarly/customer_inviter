#!/usr/bin/env ruby
# frozen_string_literal: true

$LOAD_PATH.unshift File.expand_path(File.dirname(__FILE__) + '/../lib')
require 'customer_inviter'
require 'pry'

Pry.start

puts "I was called with #{ARGV.size} arguments: #{ARGV.join(', ')}."
